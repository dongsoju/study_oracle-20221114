[6-4]

1.급여가 적은 상위 5명의순위 사번 이름 급여를 조회하시오
서브쿼리를 사용하여 작성해보시오


select *
from  ( select rank()over(order by salary) "RANK",
               dense_rank()over(order by salary) "dense_RANK",
                employee_id,
                last_name,
                salary
         from   employees
         )e
where   rownum <=5;
--2.부서별로 가장 급여를 많이 받는 사원의 사번 이름 부서번호 급여 업무코드를 조회하는 쿼리문을 작성하시오
--(단, 인라인 뷰 서브쿼리를 사용해야함)
select   e.employee_id, e.last_name, e.department_id
from     employees e,(select   department_id,  max(salary),
                      from     employeees            
                      group by department_id) k
  where    department_id IS NOT NULL





















